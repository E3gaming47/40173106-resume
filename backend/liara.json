{
  "platform": "python",
  "port": 80,
  "build": {
    "commands": [
      "pip install -r requirements.txt"
    ]
  },
  "start": {
    "command": "uvicorn main:app --host 0.0.0.0 --port 80 --workers 1"
  }
}

