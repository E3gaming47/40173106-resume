{
  "platform": "python",
  "port": 80,
  "build": {
    "commands": [
      "pip install -r requirements.txt",
      "ls -la static/ || echo 'Static directory check'"
    ]
  },
  "start": {
    "command": "uvicorn main:app --host 0.0.0.0 --port 80 --workers 1"
  }
}

