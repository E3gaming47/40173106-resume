{
  "platform": "python",
  "port": 80,
  "build": {
    "commands": [
      "pip install -r requirements.txt",
      "cp ../index.html . 2>/dev/null || true",
      "cp ../portfolio.html . 2>/dev/null || true",
      "cp ../admin.html . 2>/dev/null || true",
      "cp ../styles.css . 2>/dev/null || true",
      "cp ../portfolio.css . 2>/dev/null || true"
    ]
  },
  "start": {
    "command": "uvicorn main:app --host 0.0.0.0 --port 80 --workers 1"
  }
}

